# 微服务接口清单

> 对外接口（经 API 网关，前端/测试可见）与内部接口（服务间调用，网关屏蔽）的完整清单。
> 对外接口契约与单体版一致，详见《对外服务接口.md》；本文补充服务划分后的路由与内部契约。

## 1. 网关路由表（Nginx，编排内服务名 `backend`，端口 8000）

| 外部路径 | 转发目标（编排内服务名:端口） | 说明 |
|---|---|---|
| `/api/users/*` | `user-service:8000` | 用户注册/查询/删除 |
| `/api/auth/*` | `user-service:8000` | 登录签发 JWT、发送注册验证码 |
| `/api/courses/*` | `course-service:8000` | 课程与选课 |
| `/api/assignments/*` | `assignment-service:8000` | 作业 |
| `/api/submissions/*` | `assignment-service:8000` | 提交与评分 |
| `/api/health/` | 网关本地 | 返回 200 与网关自身状态（网关存活探活） |
| `/api/health/user/` `/api/health/course/` `/api/health/assignment/` | 对应服务 `/api/health/` | 逐服务探活（含版本号与 DB 状态） |
| `/admin/*`、`/static/user/*` | `user-service:8000` | Django 管理后台（仅用户服务暴露） |
| `/static/course/*` | `course-service:8000` | 课程服务静态资源 |
| `/static/assignment/*`、`/media/*` | `assignment-service:8000` | 作业服务静态资源与作业参考文档 |
| `/internal/*` | **一律 403** | 内部接口绝不经网关暴露 |

## 2. 对外接口（经网关，与单体版一致）

### 2.1 用户服务 user-service

| 方法 | 路径 | 说明 | 成功码 | 权限 |
|---|---|---|---|---|
| POST | `/api/users/` | 注册 `{username, password, email, verification_code, is_teacher?}` | 201 | 公开 |
| GET | `/api/users/me/` | 当前用户信息 | 200 | 已登录 |
| GET | `/api/users/` `/api/users/{id}/` | 教师/管理员看全部；学生只看自己 | 200 | 已登录 |
| DELETE | `/api/users/{id}/` | 删除用户（先跨服务级联清理课程/选课/作业/提交，成功后才删用户） | 204 | 已登录（受可见范围限制） |
| POST | `/api/auth/token/` | 登录 `{username, password}` → `{token}`（**无状态 JWT**，24h） | 200 | 公开 |
| POST | `/api/auth/send-code/` | 发送 6 位注册验证码（60s 限频、10min 有效） | 200 | 公开 |

认证方式：`Authorization: Token <jwt>`（三个服务共用同一验签逻辑，本地验签，无跨服务调用）。

### 2.2 课程服务 course-service

| 方法 | 路径 | 说明 | 成功码 | 权限 |
|---|---|---|---|---|
| POST | `/api/courses/` | 创建课程 `{name, code, description}`，`teacher_id` = 当前用户 | 201 | 仅教师 |
| GET | `/api/courses/` `/api/courses/{id}/` | 学生看全部课程；教师看自己的；详情含 `is_enrolled`、`teacher_name`、`students` | 200 | 已登录 |
| PATCH/PUT/DELETE | `/api/courses/{id}/` | 编辑/删除（删除先级联清理作业与提交） | 200/204 | 授课教师或管理员 |
| POST | `/api/courses/{id}/enroll/` | 学生选课（教师 400） | 200 | 已登录 |
| POST | `/api/courses/{id}/unenroll/` | 学生退课 | 200 | 已登录 |

### 2.3 作业与提交服务 assignment-service

| 方法 | 路径 | 说明 | 成功码 | 权限 |
|---|---|---|---|---|
| POST | `/api/assignments/` | 创建作业 `{course, title, description, deadline, reference_file?}`（课程归属经课程服务校验） | 201 | 该课程授课教师 |
| GET | `/api/assignments/` `/api/assignments/{id}/` | 支持 `?course={id}` 过滤 | 200 | 已登录 |
| PATCH/PUT/DELETE | `/api/assignments/{id}/` | 编辑/删除 | 200/204 | 该课程授课教师 |
| POST | `/api/submissions/` | 提交作业 `{assignment, code}`（选课校验经课程服务；教师 403；过期 400） | 201 | 已选课学生 |
| GET | `/api/submissions/?assignment={id}` | 学生只看自己的；授课教师看该作业全部 | 200 | 已登录 |
| POST | `/api/submissions/{id}/grade/` | 评分 `{score}`（0-100；越界 400） | 200 | 该课程授课教师 |

## 3. 内部接口（服务间调用，需请求头 `X-Internal-Key: <INTERNAL_API_KEY>`，网关屏蔽）

### 3.1 用户服务

| 方法 | 路径 | 返回 | 调用方 / 场景 |
|---|---|---|---|
| GET | `/internal/users/?ids=1,2,3` | `[{id, username, is_teacher, email, avatar, bio}]`（缺 id 的跳过） | course/assignment 服务：展示时补全教师名、学生名 |
| GET | `/internal/users/?username=demo_teacher` | 同上（按用户名查） | 演示数据脚本：定位演示教师 ID |

### 3.2 课程服务

| 方法 | 路径 | 返回 | 调用方 / 场景 |
|---|---|---|---|
| GET | `/internal/courses/{id}/` | `{id, code, name, teacher_id}`；不存在 404 | assignment 服务：创建/编辑作业、评分时校验课程存在与归属 |
| GET | `/internal/courses/{id}/students/` | `{student_ids: [...]}` | assignment 服务：提交作业前校验学生已选课 |
| GET | `/internal/courses/?teacher_id=N` | `{course_ids: [...]}` | assignment 服务：教师提交列表圈定其课程范围 |
| POST | `/internal/users/{id}/purge/` | `{deleted_enrollments, deleted_courses, course_ids}`；内部先调 assignment 服务清理这些课程的作业提交 | 用户服务：删除用户时级联清理其课程与选课 |

### 3.3 作业与提交服务

| 方法 | 路径 | 返回 | 调用方 / 场景 |
|---|---|---|---|
| POST | `/internal/courses/purge/` | `{deleted_assignments, deleted_submissions}`（按 `course_ids` 删除作业及提交） | 课程服务：删除课程时级联清理 |
| POST | `/internal/users/{id}/purge/` | `{deleted_assignments, deleted_submissions}`（删该学生的全部提交） | 用户服务：删除用户时级联清理 |

## 4. 运维接口（健康/就绪/版本，每服务一致）

| 路径 | 用途 | 响应示例 |
|---|---|---|
| `GET /api/live/` | 存活探针（进程可响应，不查 DB） | `{"status":"ok","service":"user-service","version":"dev"}` |
| `GET /api/ready/` | 就绪探针（DB 可连才 200） | `{"status":"ok","db":true,"service":"user-service","version":"abc1234"}` |
| `GET /api/health/` | 合并探活（兼容单体版路径；DB 异常返回 503） | `{"status":"ok","db":true,"service":"...","version":"..."}` |

- `version` 来自环境变量 `APP_VERSION`（CI 注入镜像版本号：提交 SHA 或 `v*` 标签），用于现场核对"跑的是哪个版本"。
- K8s：`readinessProbe` → `/api/ready/`，`livenessProbe` → `/api/live/`；网关 `depends_on` 用 `/api/health/`。
