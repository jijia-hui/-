# 教学平台数据表归属方案（v2，微服务版）

> 本文档为微服务版的最终数据归属方案（v2 重写）。v1 草案按 5 个服务划分（含独立的"认证服务/tokens 表"），并建议"单库单表、表名加前缀"，与《微服务划分图.md》v1 的 6 服务方案互相矛盾，也无法满足任务书"每张业务表明确归属、不跨服务直接读写"的要求，已废弃。
>
> 核心原则不变：**谁拥有业务，谁拥有数据**。每张表只归一个微服务所有，其他服务通过该服务的内部 API 访问，绝不直连对方的库、绝不跨服务联表。

## 1. 归属总览（与 3 个业务微服务一一对应）

| 微服务 | 数据库（同一 MySQL 实例，库级隔离） | 拥有的数据表 | 说明 |
| :--- | :--- | :--- | :--- |
| **用户服务** user-service | `otp_user` | `users`、`email_verification_codes` | 用户账号、角色（is_teacher）、注册邮箱验证码 |
| **课程服务** course-service | `otp_course` | `courses`、`enrollments` | 课程信息、选课关系 |
| **作业与提交服务** assignment-service | `otp_assignment` | `assignments`、`submissions` | 作业定义、提交记录与评分 |

与 v1 草案的差异及原因：

| v1 草案 | v2 方案 | 原因 |
| :--- | :--- | :--- |
| 认证服务拥有 `tokens` 表 | **删除该表**。登录改用无状态 JWT（HS256），Token 不落库 | JWT 由用户服务签发、三个服务本地验签，鉴权零跨服务调用、零额外存储；也无需独立认证服务 |
| 课程用 `courses.students` 多对多（自动生成的中间表） | 显式建模 `enrollments(course_id, student_id, enrolled_at)` 表 | 选课/退课是课程服务的业务动作，显式表让归属清晰、可记录选课时间；`student_id` 是**普通整数列**（不再跨表外键） |
| 建议单库单表 + 表名前缀 | **每个服务独立数据库**（`otp_user` / `otp_course` / `otp_assignment`，同一 MySQL 实例） | 任务书允许"同一数据库服务器中不同数据库或 Schema"；库级隔离从物理上杜绝跨服务联表与误访问 |
| — | `email_verification_codes` 明确归用户服务 | 注册验证码与账号生命周期强绑定，属于用户域 |

## 2. 各服务表结构（微服务版实际建表）

### 用户服务 user-service（库 `otp_user`）

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `users` | `id`, `username`, `password`(哈希), `email`, `is_teacher`, `is_staff`, `avatar`, `bio`, `created_at` | 账号是用户域核心实体，注册/登录/个人信息只有用户服务可写 |
| `email_verification_codes` | `id`, `email`, `code`, `created_at`, `used_at` | 注册验证码的签发/核销随账号注册发生，只被用户服务使用 |

### 课程服务 course-service（库 `otp_course`）

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `courses` | `id`, `name`, `code`(唯一), `description`, **`teacher_id`(整数列)**, `created_at`, `updated_at` | 课程的增删改查属于课程服务；`teacher_id` 只引用用户 ID，不建跨库外键 |
| `enrollments` | `id`, `course_id`(**库内外键** → courses), **`student_id`(整数列)**, `enrolled_at` | 选课/退课是课程服务的业务动作；对库内 `courses` 用真实外键，对用户只存 ID |

### 作业与提交服务 assignment-service（库 `otp_assignment`）

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `assignments` | `id`, **`course_id`(整数列)**, `title`, `description`, `deadline`, `reference_file`, `created_at`, `updated_at` | 作业定义由教师发布，属于课业产物域；对课程只存 ID |
| `submissions` | `id`, `assignment_id`(**库内外键** → assignments), **`student_id`(整数列)**, `code`, `status`(pending/graded), `score`, `output`, `created_at` | 提交与评分（status/score）是同一聚合、同一事务，归作业与提交服务 |

> 单体版数据库 `online_teach` 及其外键级联保留不动（Git 标签 `monolith-start`），供性能对比实验使用；微服务版使用上面三个新库，互不影响。

## 3. 跨服务数据关联方式（只存 ID，不联表）

```
┌────────────────────────┐
│ user-service（otp_user）│
│ users(id, username,    │
│       is_teacher, ...) │
└──────┬─────────────────┘
       │ 被以下列以【纯整数 ID】引用（无外键、无联表）：
       │   courses.teacher_id、enrollments.student_id、
       │   submissions.student_id
       │ 需要用户名等展示信息时：调 user-service 内部接口
       │   GET /internal/users/?ids=1,2,3（批量）
▼
┌────────────────────────┐        course_id（纯整数 ID）
│ course-service         │──────────────────────────┐
│ （otp_course）         │                          ▼
│ courses(teacher_id)    │              ┌─────────────────────────┐
│ enrollments(           │              │ assignment-service      │
│   course_id FK 库内,   │◄────────────│ （otp_assignment）      │
│   student_id)          │ 作业归属/选课 │ assignments(course_id)  │
└────────────────────────┘ 校验走内部   │ submissions(            │
                          接口，见下表   │   assignment_id FK 库内,│
                                        │   student_id)           │
                                        └─────────────────────────┘
```

### 关键规则

1. **单一所有权**：每张表只有一个 Owner 服务，只有它能增删改；其他服务读也只能走该服务的内部 API。
2. **ID 引用**：跨服务关联只存对方主键（普通 `BigIntegerField`，建普通索引），**不用数据库外键**（外键会跨库失效，也会诱导联表）；服务内部（如 `enrollments.course_id`、`submissions.assignment_id`）保留库内外键与级联。
3. **不跨服务联表**：库级隔离从物理上杜绝；所有需要"对方数据"的场景都通过内部接口完成（清单见《微服务接口清单.md》，失败处理见《跨服务调用说明.md》）。
4. **级联删除走编排**：删除用户/课程时由 Owner 服务按顺序调用下游服务清理（见《跨服务调用说明.md》第 4 节），替代原数据库外键级联。
5. **最小暴露**：内部接口只暴露业务所需的最小字段（id、username、is_teacher 等），且必须携带内部密钥 `X-Internal-Key`，网关层对 `/internal/*` 一律拒绝。

## 4. 数据库部署策略

| 环境 | 做法 |
| :--- | :--- |
| Docker Compose（`docker-compose.micro.yml`） | 一个 MySQL 8 容器，首次启动经 `/docker-entrypoint-initdb.d/init.sql` 一次创建 `otp_user`、`otp_course`、`otp_assignment` 三库并授权 |
| Kubernetes（`k8s/micro/`） | 一个 MySQL Deployment + PVC，init SQL 经 ConfigMap 挂载；三个业务服务各自 `DB_NAME` 指向自己的库 |
| 本地开发 / CI 测试 | `USE_SQLITE=1` 时各服务用本地 SQLite 跑测试，无需 MySQL |

> 迁移：每个服务独立执行 `python manage.py migrate`（容器 entrypoint 自动完成），迁移文件随服务代码走，互不感知。
