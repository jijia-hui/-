# 教学平台数据表归属方案

> 本文档定义教学平台各微服务的数据表归属关系，遵循"谁拥有业务，谁拥有数据"的核心原则。
>
> 每张表只归一个微服务所有，其他服务通过 API 访问而非直连数据库。

---

## 归属总览

| 微服务 | 拥有的数据表 | 说明 |
| :--- | :--- | :--- |
| **用户服务** (User Service) | `users` | 用户账号、角色、个人信息 |
| **认证服务** (Auth Service) | `tokens` | 登录凭证、Token 记录 |
| **课程服务** (Course Service) | `courses`、`enrollments` | 课程信息、选课关系 |
| **作业服务** (Assignment Service) | `assignments` | 作业定义、截止时间 |
| **提交服务** (Submission Service) | `submissions` | 学生提交记录、评分 |

---

## 各服务详细表结构

### 用户服务 (User Service)

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `users` | `id`, `username`, `password_hash`, `email`, `is_teacher`, `created_at` | 用户是核心实体，账号信息只由用户服务管理 |

### 认证服务 (Auth Service)

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `tokens` | `id`, `user_id`, `token`, `expires_at`, `created_at` | Token 是认证领域的核心，与登录/登出生命周期绑定 |

### 课程服务 (Course Service)

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `courses` | `id`, `name`, `code`, `description`, `teacher_id`, `created_at` | 课程由教师创建，属于课程领域 |
| `enrollments` | `id`, `course_id`, `student_id`, `enrolled_at` | 选课关系是课程服务的业务逻辑 |

### 作业服务 (Assignment Service)

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `assignments` | `id`, `course_id`, `title`, `description`, `deadline`, `created_at` | 作业依附于课程，由教师发布 |

### 提交服务 (Submission Service)

| 表名 | 主要字段 | 归属理由 |
| :--- | :--- | :--- |
| `submissions` | `id`, `assignment_id`, `student_id`, `code`, `status`, `score`, `submitted_at` | 提交和评分是独立业务域 |

---

## 跨服务数据关联方式

微服务之间 **不直接连对方的数据库**，而是通过外键 ID 引用 + API 调用：

```
┌─────────────┐       ┌─────────────┐
│  User 服务   │       │  Auth 服务   │
│             │       │             │
│  users 表    │◄──────│  tokens 表   │
│  (id, name) │ user_ │  (user_id)  │
└──────┬──────┘  id   └─────────────┘
       │
       │ teacher_id / student_id
       ▼
┌─────────────┐       ┌──────────────┐
│  Course 服务 │       │ Assignment   │
│             │       │   服务        │
│  courses 表  │◄──────│ assignments  │
│  (id, name) │course_│  (course_id) │
│             │  id   │              │
│ enrollments │       └──────┬───────┘
│ (course_id, │              │
│  student_id)│              │ assignment_id
└─────────────┘              ▼
                      ┌──────────────┐
                      │ Submission   │
                      │   服务        │
                      │ submissions  │
                      │(assignment_id│
                      │ student_id)  │
                      └──────────────┘
```

### 关键规则

- 表 A 引用表 B 时，只存 `B.id`（外键 ID），不存 B 的其他字段
- 需要展示 B 的信息时，调用 B 所属服务的 API 获取
- 每个服务拥有独立的数据库（或独立的 schema）

---

## 数据库部署策略

根据项目规模选择：

| 策略 | 做法 | 适用场景 |
| :--- | :--- | :--- |
| **共享数据库，独立 Schema** | 一个数据库实例，每个服务一个 schema | 项目初期，开发阶段 |
| **独立数据库** | 每个服务一个数据库实例 | 生产环境，需要隔离 |
| **单库单表（当前推荐）** | 所有表放一个库，但逻辑上按服务分组 | 当前项目规模，后续拆分 |

> **当前建议：** 项目处于开发阶段，先用 **单库 + 逻辑分组** 即可，表名前加服务前缀（如 `user_users`、`course_courses`），后续拆分微服务时迁移成本最低。

---

## 归属原则总结

1. **单一所有权**：每张表只有一个服务是 "Owner"，只有它能增删改
2. **ID 引用**：跨服务关联只存 ID，不冗余字段
3. **API 通信**：需要跨服务数据时走 API，不直连数据库
4. **业务内聚**：表的归属跟着业务走，谁负责这个业务逻辑，谁就拥有这张表
5. **最小暴露**：每个服务只暴露必要的查询接口给其他服务
