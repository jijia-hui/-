# 每日站会简报 —— 2026-08-31（第 6 天）

**小组**：杨任宇班-17组　**日期**：2026-08-31

**昨天做了什么**：8/29 中期检查——原系统用例运行与 Git 标签（monolith-start / v1.0.0）、三层图与追溯表、容器与 CI、微服务方案四项；当日修复邮件半配置导致注册验证码 502 的问题（`59a304a`）；补齐中期检查所缺项目（CD/K8s）。

**今天做什么**：
1. 本地 Kubernetes 集群就绪：启用 Docker Desktop 内置 K8s（kubeadm 单节点 v1.36.1）。期间解决两个问题——新版 Docker Desktop 需从界面创建集群；Docker Hub 直连拉取 `kindest/node` 失败，改用 daocloud 镜像加速 + 预拉镜像（`kindest/node`、`mysql:8.0`、GHCR 前后端镜像）后创建成功；
2. 平台全栈部署到 K8s（`k8s/*.yaml` + `scripts/deploy_k8s.sh`）：mysql / backend / frontend 三个 Deployment + Service + PVC 全部 Running，前端 NodePort 30080 入口健康检查通过（首页 200、`/api/health/` db:true、鉴权 403），演示数据已初始化（demo_teacher / demo_student）。T-M-02-G 的 K8s 清单完成本体验证，为 T-M-02-CI 自动部署与 T-M-04 扩缩容实验搭好环境；
3. 中期检查材料归档：按任务书四项要求整理 46 个文件至 `D:\大二小学期\中期检查目录`，附目录说明与 Git 标签证据文档；

**现在卡在哪里**：无阻塞。待推进：微服务拆分骨架（T-M-02）今日到期，各服务工程需按 `微服务划分图.md` 落地；当前 K8s 部署为脚本手动触发，CD 自动对接（T-M-02-CI）明日启动。

**明日计划**（9/1，进入微服务阶段第 2 天）：完成 4 个微服务工程骨架收尾（T-M-02）；nginx 网关路由联调（T-M-02-G 剩余部分）；多服务 matrix CI/CD 流水线（T-M-02-CI）；跨服务调用说明文档（T-M-07）；前端 API 适配网关（T-M-09）启动。
