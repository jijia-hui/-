# 杨任宇班-17组 · 站会简报

**日期**：2026-08-31（后 5 天第 1 天）

中期检查已过，看板里中期任务都进了 Done。从今天起进入后半段：把单体后端按业务拆成微服务。

## 今日进展

看板侧把后 5 天的拆分任务铺开，先推进用户服务和课程服务，作业服务留到后面。

代码上完成两刀，均已推到 `check8.27`：

- **用户服务**（T-M-02a，`50f3bee`）：独立进程承接注册、登录和 Admin。Nginx 把 `/api/auth/`、`/api/users/`、`/admin/` 转到 `user-service`。
- **课程服务**（T-M-02b，`43d6565`）：课程增删改查、选课/退课单独跑。顺带加了内部接口（查是否已选课、查授课教师），方便下一步作业服务调用，而不去直连课程库。

单元和集成测试共 150 项通过。当前 Compose 里是：MySQL、user-service、course-service、仍负责作业/提交的 backend，以及前端。

## 当前待克服的困难

开发本身没有卡住。主要是三件事还没落地：

1. 作业、提交、评分还在原来的 `backend` 上，**assignment-service 尚未拆出**。
2. 本机 Docker 拉镜像遇到过 TLS 证书问题，今天的拆分主要靠仓库配置和测试保证，Compose 一键启动还没在本机完整复核。
3. 今天两次提交只在 `check8.27`，**`main` 还没同步**。

## 下一步

优先拆 **assignment-service**（作业 / 提交 / 评分和媒体文件）。学生提交前改为调用课程服务的内部选课接口。若顺利，再收口多服务 Compose 和各服务的 K8s Deployment。
