# 每日站会简报 —— 2026-08-29（第 5 天，中期检查日）

**小组**：杨任宇班-17组　**日期**：2026-08-29

**昨天做了什么**：注册改为邮箱验证码、Docker Compose 健康检查与 CI/CD 优化（`7f4b73d`）；重构 GitHub Projects 看板（卡片统一 `[T-编号]`、补 Target date / Priority），同步仓库看板文档与统计页面。组件级/对象级顺序图 0/14，是中期检查最大缺口。

**今天做什么**（中期检查日，全天围绕补缺口与迎检）：
1. **补齐三层图缺口**（`39107cb`）：组件级顺序图 COMP-SEQ01~14、对象级顺序图 OBJ-SEQ01~14 全部完成，追溯表更新至 v1.3；
2. **完成微服务设计三件套**（T-M-01）：微服务划分图（3 个业务微服务：user / course / assignment）、对外服务接口清单、数据表归属方案（`4804db8`~`4c79912`），并 merge 进送审分支 `check8.27`（`04f8faf`）；
3. **中期检查**：四项要求全部验收通过——①用例运行 + Git 标签（monolith-start / v1.0.0），②三层图 + 追溯表，③容器启动 + CI 自动构建测试，④微服务方案。检查材料汇编为 `05_management/中期检查汇报.md`（`35e9806`）；
4. 检查后追加两项工程改进：拉取式部署 `docker-compose.prod.yml`（直连 GHCR 镜像一键运行，`8c613c0`）；

**现在卡在哪里**：无阻塞。中期检查遗留整改项；检查后发现的验证码 502 问题当日已修复并经流水线验证。

**明日计划**：8/30 周日不安排集中实践。8/31（第 6 天）首先进行中期检查补足，然后进入微服务阶段：按微服务划分图启动 4 个服务工程骨架（T-M-02）、nginx 网关路由 + K8s 多服务清单初稿（T-M-02-G）；本地先行搭建 Kubernetes 集群环境，为自动部署与扩缩容实验做准备。
