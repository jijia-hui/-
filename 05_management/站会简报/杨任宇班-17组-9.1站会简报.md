# 杨任宇班-17组 · 站会简报

**日期**：2026-09-01（后 5 天第 2 天）

中期后的拆分主线今天收口：三个业务服务已经独立进程，CI/CD 和 K8s 清单也合进 `check8.27`。

## 今日进展

- **作业服务**（T-M-02c，`4e35357`）：作业、提交、评分和媒体从原来的 `backend` 拆出为 `assignment-service`。学生提交前走课程服务内部选课接口，不再直连课程库。Compose 已去掉 `backend`。
- **部署收口**（T-M-02，`129eb52`）：push 到 `check8.27` 也会跑 CI；CI 校验五服务；K8s 分别为 user / course / assignment；本机可用 `python scripts/verify_gateway.py` 验 8080 网关。
- 看板同步：02a/02b/02c/T-M-02 进完成；下一步 **T-M-03** 微服务版用例回归放到准备好了。

单测+集成约 153 项通过（`SERVICE_ROLE=all`）。当前 Compose 五服务：MySQL、user-service、course-service、assignment-service、前端（Nginx 8080）。

## 当前待克服的困难

1. 本机 Docker 拉镜像曾有 TLS 证书问题，**Compose 一键启动仍由组长在本机复核**。没有 8080 就做不了经网关的 UC 回归。
2. 今天提交只在 `check8.27`，`main` **还没同步**。
3. 仍共用一个 MySQL，不是每服务一库。

## 下一步

组长本机 `docker compose up -d --build` 跑通后，组员取 **T-M-03**：经 8080 走通 UC01～UC14，并跑 `python scripts/verify_gateway.py`。9/2 再取 HPA 与故障实验。