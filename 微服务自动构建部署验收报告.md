# 微服务自动构建与部署 · 验收报告

- **对应任务**：《软件工程基础实践》后 5 天任务 2——"让微服务也能自动构建和部署"
- **验收日期**：2026-09-01
- **验收方式**：全部结论基于**实际运行结果**（GitHub Actions 运行记录 API 取证、D:\test4 免克隆拉取式部署实测、全量 E2E 回归实测），非文档自述。
- **验收环境**：
  - 流水线：GitHub Actions（ubuntu-latest + kind 集群）
  - 本地实测：Windows 11 + Docker Desktop（Engine 29.7.2），`D:\test4` 仅含 1 个下载的编排文件，无源码克隆

---

## 要求 1：代码提交后，相关微服务自动完成构建、测试、制作镜像和部署 —— ✅ 达成

### 1.1 流水线设计（`.github/workflows/ci-micro.yml` / `cd-micro.yml`）

提交到 `main` 后自动执行（按路径检测，只重做**发生变更**的服务）：

```
push → 变更检测（git diff 按目录判定 user/course/assignment/gateway/编排）
     → （变更服务）单元+API 测试（SQLite，失败即止）
     → （变更服务）构建并推送版本化镜像到 GHCR（版本号=提交 SHA / v* 标签 + latest）
     → kind 集群部署（k8s/micro/，逐服务 rollout + 健康检查 + 版本号核对 + /internal 屏蔽校验）
     → 全量 E2E 回归（UC01~UC14 + 级联清理，58 项断言）
     → 产物归档（部署日志 / E2E 报告 / 失败诊断：Pod 状态 + describe + 各服务日志）
```

任一环节失败即停止，后续部署不执行（任务书要求）。打 `v*` 标签时版本号取标签名并视为全部服务变更。

### 1.2 实测证据：CD-Micro 运行历史（GitHub API 取证，2026-09-01）

| 运行 ID | 提交 | 结论 | 说明 |
|---|---|---|---|
| 33382400841 | 5358642 | ❌ failure | 首次运行，E2E 步骤失败（排查过程见 §3.4 案例 A） |
| 33383200990 | b80d2a7 | ✅ success | 修复后复绿 |
| 33462976899 | 7463d8e | ✅ success | 编排自包含修复 |
| 33468539525 | 5904c41 | ✅ success | 组员提交：故障注入脚本（自动触发、自动通过） |
| 33468646291 | bd05bd8 | ✅ success | 组员提交：故障演练流水线 |
| 33469238973 | 94bdb3f | ✅ success | 组员提交：user-service HPA |

> 组员在 9/1 的每个提交（HPA、故障演练相关）都被流水线自动处理并全部通过——"代码提交后自动构建、测试、制作镜像和部署"的**活证据**。（94cb555 一条被并发取消属正常去重行为。）

### 1.3 实测证据：免克隆拉取式部署（D:\test4）

在空目录 `D:\test4` 仅放入下载的 `docker-compose.micro.prod.yml`（6.6KB，单文件自包含，MySQL 三库初始化 SQL 以 compose `configs` 内联）：

```bash
docker compose -f docker-compose.micro.prod.yml up -d
```

- 自动拉取 5 个 GHCR 镜像：`otp-user-service`、`otp-course-service`、`otp-assignment-service`、`otp-gateway`（CD-Micro 构建推送）、`online-teaching-platform-frontend`（CD 构建推送）；
- 6 个容器按依赖顺序全部 healthy：MySQL（自动建三库）→ 三个业务服务（各自迁移/收集静态文件）→ 网关 → 前端；
- 镜像即流水线产物：**otp-\* 镜像只能来自 CD-Micro 的构建推送，其成功拉取与运行本身即"自动构建+制作镜像+部署"链路打通的直接证明**。

---

## 要求 2：所有后端公开接口都要有 API 测试；全部业务场景（用例）端到端回归 —— ✅ 达成

### 2.1 公开接口 × API 测试映射（服务级测试，共 98 项，全部通过）

| 服务 | 接口 | API 测试（文件） | 结果 |
|---|---|---|---|
| user | POST /api/users/（注册：验证码必填/正确核销/教师标记/重复用户名/缺邮箱） | `tests/test_auth.py`、`test_verification.py` | ✅ 34/34 |
| user | POST /api/auth/token/（登录：签发 JWT+角色 claims/错密码/未注册/缺字段/过期 Token） | `tests/test_auth.py` | ✅ |
| user | GET /api/users/me/、GET /api/users/（列表可见性：教师全部/学生仅自己）、GET /api/users/{id}/（越权 404/本人 200） | `tests/test_auth.py` | ✅ |
| user | DELETE /api/users/{id}/（级联清理成功 204；下游失败 502 且用户保留；越权 404） | `tests/test_delete_cascade.py` | ✅ |
| user | POST /api/auth/send-code/（正常签发/非法邮箱/已注册/60s 限频 429） | `tests/test_verification.py` | ✅ |
| course | /api/courses/ CRUD（教师创建归属/学生 403/编号重复 400/列表可见性/详情 is_enrolled/编辑越权/删除级联+失败 502） | `tests/test_courses_api.py` | ✅ 27/27 |
| course | /api/courses/{id}/enroll、/unenroll（幂等/教师 400） | `tests/test_courses_api.py` | ✅ |
| course | 用户名补全降级（用户服务不可用 → teacher_name=null 且数据正常返回） | `tests/test_degrade.py` | ✅ |
| assignment | /api/assignments/ CRUD（归属校验经课程服务：非本人 403/课程不存在 400/服务不可用 503） | `tests/test_assignments_api.py` | ✅ 37/37 |
| assignment | POST /api/submissions/（教师 403/未选课 403/**选课校验服务不可用 503 不绕过**/过期 400） | `tests/test_submissions.py` | ✅ |
| assignment | GET /api/submissions/（学生仅自己/教师范围圈定/圈定失败 503/按作业过滤） | `tests/test_submissions.py` | ✅ |
| assignment | POST /api/submissions/{id}/grade/（本人 200 graded/学生 403/非授课 403/越界 400/缺分 400/服务不可用 503） | `tests/test_submissions.py` | ✅ |
| 三服务 | /api/health/、/api/live/、/api/ready/ 探针 | E2E 前置断言（§2.2） | ✅ |

> PUT 与 PATCH 共用同一处理器路径（视图 `update()`），由 PATCH 测试覆盖；跨服务内部接口另有契约测试（`test_internal.py` ×3）。

### 2.2 全部业务场景（用例）端到端回归 —— 两轮实测均 58/58 通过

`04_tests/e2e/run_e2e_micro.py` 经 API 网关覆盖 UC01~UC14 全部用例 + 微服务特有的跨服务级联删除，共 58 项断言：

| 运行 | 环境 | 结果 | 报告 |
|---|---|---|---|
| 2026-08-31 | CD-Micro 流水线内（kind 集群） | 58/58 | Actions 构建产物（run 33383200990） |
| 2026-09-01 | **D:\test4 免克隆拉取栈实测** | **58/58** | `04_tests/reports/e2e_micro_report_20260901_154118.md` |

（过程记录 `e2e_micro_report_20260831_180702.md` 为 57/58 的失败现场，见 §3.4 案例 C。）

### 2.3 重点展示：3 个代表性用例（2026-09-01 test4 实测逐项摘录）

**① UC01 用户注册（user-service 独立域）**
```
PASS  E2E-TC01 学生注册: HTTP 201
PASS  E2E-TC01 教师注册: HTTP 201
PASS  E2E-TC01-异常 缺验证码注册被拒: HTTP 400
PASS  E2E-TC01-异常 缺邮箱注册被拒: HTTP 400
```
覆盖主成功流程（学生/教师）+ 异常流程（缺验证码/缺邮箱），验证码经容器内注入避免真实 SMTP。

**② UC09 学生提交作业（assignment→course 跨服务校验，微服务化最有代表性的用例）**
```
PASS  E2E-TC09 学生提交作业: HTTP 201
PASS  E2E-TC09 提交状态为 pending
PASS  E2E-TC09 学生用户名经用户服务补全          ← 跨服务展示补全（assignment→user）
PASS  E2E-TC09-异常 教师提交被拒: HTTP 403
PASS  E2E-TC09-异常 过期作业提交被拒: HTTP 400
（另有单元级：选课校验服务不可用 → 503 快速失败、绝不绕过校验）
```
提交链路横跨三个服务：JWT 本地鉴权 → 本地查作业/判截止 → **course-service 选课校验（fail-closed）** → 落库 pending → 响应补全学生名（**user-service，失败降级**）。

**③ UC12 教师评分 + 用户删除级联清理（跨服务写一致性）**
```
PASS  E2E-TC12 教师评分: HTTP 200
PASS  E2E-TC12 评分结果 graded/92
PASS  E2E-TC12-异常 学生评分被拒: HTTP 403
PASS  E2E-TC12-异常 分数越界被拒: HTTP 400
PASS  E2E-清理 删除测试账号 e2e_stu_248472（级联清理课程/作业/提交）: HTTP 204
PASS  E2E-清理 删除测试账号 e2e_tea_248472（级联清理课程/作业/提交）: HTTP 204
PASS  E2E-清理 教师课程已级联删除: HTTP 404     ← 三服务数据最终一致
```
评分前的授课教师校验经 course-service；用户删除触发三服务编排清理（user→course→assignment），清理后课程确实消失，验证了替代数据库外键级联的跨服务一致性。

---

## 要求 3：服务日志、健康检查、就绪检查、版本号 + 部署失败排查 —— ✅ 达成

### 3.1 四项可观测能力（test4 实测输出）

| 能力 | 入口 | 实测 |
|---|---|---|
| 服务日志 | `docker logs otp-micro-user --tail` / K8s `kubectl logs deploy/user-service` | ✅ gunicorn 访问/错误日志、跨服务调用 WARN（目标+耗时+重试次数）可查 |
| 健康检查 | `GET /api/health/`（网关聚合 + `/api/health/{user,course,assignment}/` 逐服务） | ✅ 全部 200，含 DB 状态 |
| 就绪检查 | `GET /api/ready/`（DB 可连才 200；K8s readinessProbe） | ✅ `{"status":"ok","db":true,...}` |
| 存活检查 | `GET /api/live/`（不查 DB；K8s livenessProbe，DB 抖动不误杀） | ✅ |
| **版本号** | 探针响应 `version` 字段 = `APP_VERSION` 环境变量 = **镜像标签** | ✅ `"version":"latest"`（test4）；CI 注入 SHA/标签，可现场与 GHCR/流水线对账 |

### 3.2 逐服务探活实测（经前端 → 网关，test4）

```
/api/health/            -> 200   （网关）
/api/health/user/       -> 200   {"service":"user-service","version":"latest"}
/api/health/course/     -> 200   {"service":"course-service","version":"latest"}
/api/health/assignment/ -> 200   {"service":"assignment-service","version":"latest"}
```

### 3.3 版本化镜像（任务书：不能只依赖 latest）

CD 每次运行同时推送 `版本号`（提交 SHA / `v*` 标签）与 `latest`；K8s 清单以 `__TAG__` 占位、部署脚本替换为版本号，`/api/health/` 返回的版本号即部署版本。

### 3.4 部署失败是怎么查出来的 —— 三个真实案例（全部为本次实践实际发生）

**案例 A（流水线级）：CD-Micro 首次运行失败——port-forward 生命周期**
- 现象：run 33382400841 中"部署并健康检查"步骤**全绿**，下一步 E2E 却全部 `Connection refused`（连 127.0.0.1:8080 失败）。
- 排查：Actions 日志对比两步骤 → 部署脚本用 `trap` 在退出时清理了自己的 kubectl port-forward，而 E2E 是下一个 step 才执行 → 通道已被销毁。集群本身健康（rollout/健康检查全过），问题在**步骤间资源生命周期**而非应用。
- 修复：在部署与 E2E 之间新增独立的后台 port-forward 步骤（作业级存活 + 就绪探测），提交 b80d2a7 复跑全绿。
- 方法论：**看"哪些步骤绿、哪个步骤红"界定问题域，再读红步骤首条错误**——不是应用代码问题，是流水线编排问题。

**案例 B（供应链级）：GHCR `frontend:latest` 被实验版本覆盖**
- 现象：test4 经前端的 `/api/health/` 返回的竟是 **assignment-service** 的 health 响应；`/api/health/user/` 返回 Django 404；网关访问日志中**完全没有这些请求**。
- 排查（三分定位法）：① 同路径直连网关 8000 → 200，排除网关/服务问题；② 响应头含 Django corsheaders 的 `Vary: origin`，说明请求到了某个业务服务而非网关；③ 进前端容器 cat nginx.conf → 发现是"前端直连三微服务"的**另一套实验配置**（非仓库任何版本），且镜像创建时间对应当天上午——**GHCR 的 frontend:latest 被一份实验构建覆盖**（团队并行实验未走分支/标签隔离）。
- 修复与加固：test4 改用本地已验证镜像后全链路复绿（探活/登录/E2E 58/58）；**根本教训：部署必须固定版本标签**——这恰是任务书"镜像要有版本号，不能只使用 latest"的现实印证。CI 已同时推送 SHA 版本标签可供固定（待团队确认后统一将部署标签从 latest 切到 SHA，并约定实验镜像禁止覆盖 latest）。

**案例 C（应用级）：容器启动即 unhealthy**
- 现象：首次 `docker compose up` 三个业务服务全部 unhealthy、循环重启。
- 排查：`docker logs otp-micro-user` 直接看到 Python 栈 `Error loading MySQLdb module. Did you install mysqlclient?`——服务用 PyMySQL，缺 `pymysql.install_as_MySQLdb()` 兼容垫片（单体工程在 `web_backend/__init__.py` 有、新建的三个服务漏了）。
- 修复：三个服务的 `config/__init__.py` 补垫片，重建镜像恢复。1 分钟定位到修复，全程日志可查。

---

## 结论与遗留事项

| 验收项 | 结论 |
|---|---|
| 1. 提交后自动构建/测试/镜像/部署（按变更服务） | ✅ 达成（6 次真实运行记录，含组员提交自动触发） |
| 2. 公开接口全覆盖 API 测试 + 全用例 E2E 回归 | ✅ 达成（服务级 98 项全绿；E2E 58/58 两轮实测；3 个代表性用例见 §2.3） |
| 3. 日志/健康/就绪/版本号 + 失败排查演示 | ✅ 达成（§3.1–3.4，三个真实排查案例） |

**遗留事项（需团队决策，不阻塞验收）**：
1. GHCR `online-teaching-platform-frontend:latest` 当前为某成员的实验构建（前端直连微服务、无网关），**与主干的网关架构冲突**；请相关成员确认后重推正确版本，或团队统一改用"前端直连"方案（二者架构取其一，避免混用）。
2. 建议所有部署入口（compose prod / k8s）统一固定 SHA 版本标签，实验镜像禁止推送 `latest`。
3. 本机 Docker Desktop 在内存紧张时出现过引擎假死/容器 DNS 错乱（重启电脑恢复），演示前建议预留内存或重启 Docker。
