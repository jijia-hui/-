# 在线教学与实训平台 - 部署文档
## 0. 容器化部署（推荐）
使用 Docker Compose 一键启动 MySQL、后端（Django + gunicorn）、前端（Nginx 托管静态资源并反向代理 API），
无需在服务器上手工安装 Python / Node / MySQL / Nginx。

### 0.1 环境要求
- Docker Engine 24+ 与 Docker Compose v2（Windows 上安装 Docker Desktop 即可）
- 内存 4GB 以上

### 0.2 项目文件说明
```text
online_teaching_platform/
├── docker-compose.yml      # 容器编排（mysql / backend / frontend 三个服务）
├── .env                    # 邮箱等配置（compose 自动读取用于变量替换）
├── web_backend/
│   ├── Dockerfile          # 后端镜像（Python 3.12 + gunicorn）
│   ├── entrypoint.sh       # 启动脚本：等 MySQL → migrate → collectstatic → gunicorn
│   └── requirements.txt    # Python 依赖
└── web_frontend/
    ├── Dockerfile          # 前端镜像（Node 构建 → Nginx 托管）
    └── nginx.conf          # 反向代理 /api → 后端，托管 /media /static 与 SPA
```

### 0.3 启动
```bash
cd online_teaching_platform
docker compose up -d --build
```
- 首次启动会自动创建 MySQL 数据库 `online_teach`（用户 `teach_user`）、执行迁移并收集静态文件。
- 访问 `http://localhost:8080` 即可使用系统（默认端口 8080，可用 `WEB_PORT` 修改）。
- 查看日志：`docker compose logs -f backend`（或 `frontend`、`mysql`）。
- 初始化测试数据：`docker compose exec backend python manage.py seed_data`（幂等，创建演示账号 `demo_teacher` / `demo_student`，密码 `Demo@1234`，以及示例课程与作业）。

### 0.4 可配置项（写入根目录 .env，或直接编辑 docker-compose.yml）
| 变量 | 默认值 | 说明 |
|---|---|---|
| WEB_PORT | 8080 | 前端对外访问端口 |
| DB_PASSWORD | teach_pass_2026 | MySQL 业务用户 teach_user 的密码 |
| DB_ROOT_PASSWORD | root_pass_2026 | MySQL root 密码 |
| DJANGO_SECRET_KEY | （内置开发密钥） | 生产环境务必设置 |
| DJANGO_DEBUG | False | 是否开启调试模式 |
| DJANGO_ALLOWED_HOSTS | * | 允许访问的主机，逗号分隔 |
| EMAIL_HOST_USER / EMAIL_HOST_PASSWORD | 来自 .env | 邮件配置，缺省时邮件打印到控制台 |

### 0.5 数据持久化
- 数据库、上传文件（media）、静态文件（staticfiles）分别使用命名卷 `mysql_data`、`media_data`、`static_data` 持久化，
  `docker compose down` 不会丢失数据；彻底清理（删除数据）需加 `-v`。

### 0.6 常用运维命令
```bash
docker compose ps            # 查看服务状态
docker compose logs -f       # 跟踪所有服务日志
docker compose restart backend
docker compose down          # 停止（保留数据）
docker compose up -d --build # 代码变更后重新构建并启动
```

### 0.7 创建超级管理员（可选，用于 Django 后台）
```bash
docker compose exec backend python manage.py createsuperuser
```
后台地址：`http://localhost:8080/admin/`（需先设置 DJANGO_ALLOWED_HOSTS 为实际域名/IP）。

---
## 1. 环境要求（手工部署，备用方案）
### 1.1 服务器要求
操作系统：windows 10/11

CPU：2核心及以上

内存：4GB及以上（推荐8GB）

硬盘：20GB及以上（用于存储上传文件和数据库）

网络：公网IP或域名，开放80/443端口（HTTP/HTTPS）

### 1.2 软件依赖
|软件|	版本|	说明|
|---|---|---|
|Python	|3.10+	|后端运行环境|
|Node.js|	18.x / 20.x|	前端构建环境|
|MySQL|	5.7 / 8.0|	数据库|
|Nginx|	1.18+|	反向代理与静态文件服务|
|Git|	最新版|	代码拉取|
|pip|	最新版|	Python包管理|
|npm / yarn|	最新版|	前端包管理|

## 2. 部署前准备
### 2.1 克隆代码
bash
git clone <https://github.com/jijia-hui/-.git> online_teaching_platform
cd online_teaching_platform
项目结构（简要）：

text
online_teaching_platform/
├── web_backend/          # Django 后端主目录
│   ├── web_backend/      # 项目配置
│   ├── apps/             # 主要应用（用户、课程、作业、提交）
│   ├── manage.py
│   ├── requirements.txt  # 请确保存在
├── web_frontend/         # React 前端项目（Vite）
│   ├── src/
│   ├── package.json
│   ├── vite.config.js
│   └── ...
...

### 2.2 安装 Node.js (如果未安装)
```bash
# 使用 nvm 安装（推荐）
curl -o- https://raw.githubusercontent.com/nvm-sh/nvm/v0.39.7/install.sh | bash
source ~/.bashrc
nvm install 20
nvm use 20
```
## 3. 后端部署
### 3.1 创建 Python 虚拟环境
```bash
cd web_backend
python3 -m venv venv
source venv/bin/activate
```
### 3.2 安装 Python 依赖
创建 requirements.txt（若没有，可手动创建并包含以下内容）：

```text
Django==5.0.6
djangorestframework==3.15.1
django-cors-headers==4.3.1
mysqlclient==2.2.4
pymysql==1.1.0
```
安装：

```bash
pip install -r requirements.txt
```
### 3.3 配置数据库
登录 MySQL 创建数据库和用户：

```sql
CREATE DATABASE online_teach CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
CREATE USER 'teach_user'@'localhost' IDENTIFIED BY 'your_strong_password';
GRANT ALL PRIVILEGES ON online_teach.* TO 'teach_user'@'localhost';
FLUSH PRIVILEGES;
```
修改 web_backend/web_backend/settings.py 中的数据库配置：

```python
DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'online_teach',
        'USER': 'teach_user',
        'PASSWORD': 'your_strong_password',
        'HOST': 'localhost',
        'PORT': '3306',
    }
}
```
### 3.4 配置静态文件和媒体文件
在 settings.py 中设置：

```python
STATIC_URL = '/static/'
STATIC_ROOT = os.path.join(BASE_DIR, 'staticfiles')

MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')
```
### 3.5 配置跨域和允许的主机
```python
ALLOWED_HOSTS = ['your-domain.com', 'www.your-domain.com', '服务器IP']
CORS_ALLOWED_ORIGINS = [
    'https://your-domain.com',
    'http://localhost:5173',  # 开发时可保留
]
```
### 3.6 生成密钥与迁移
```bash
# 生成新的 SECRET_KEY (替换 settings.py 中的默认值)
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"

# 执行数据库迁移
python manage.py makemigrations
python manage.py migrate

# 创建超级管理员（可选，用于后台管理）
python manage.py createsuperuser
```

### 3.7 测试运行
```bash
python manage.py runserver 0.0.0.0:8000
通过浏览器访问 http://服务器IP:8000 验证后端接口（如 /api/courses/ 应返回401，表示需要认证）。
```
## 4. 前端部署（构建）
### 4.1 安装依赖
```bash
cd ../web_frontend   # 进入前端目录
npm install
```
## 4.2 配置 API 地址
修改 src/api/client.js 中的 baseURL：

```javascript
const api = axios.create({
  baseURL: '/api',   // 生产环境使用 Nginx 代理，保持相对路径
  // 或使用绝对路径 baseURL: 'https://your-domain.com/api'
});
```

## 5.  结语
按照以上步骤，您可以将在线教学与实训平台成功部署到生产环境。如有任何问题，请检查日志并根据错误信息调整配置。建议定期更新系统补丁和软件包，保持环境安全稳定。

## 6. CI/CD 自动化流水线（GitHub Actions）
代码托管在 GitHub（`github.com/jijia-hui/-`），提交后自动触发 `.github/workflows/` 中的流水线，无需本地运行测试。

### 6.1 CI（`ci.yml`）——每次推送到 main / PR 到 main 时触发
| Job | 内容 | 失败即阻断 |
|---|---|---|
| 后端测试 | 启动 MySQL 8 服务容器 → 单元测试（UNIT-TC01~14）+ 集成测试（INT-TC01~14）→ 迁移 → 端到端测试（E2E-TC01~14，47 项断言） | 是 |
| 前端构建 | `npm ci` → `npm run build`（lint 仅提示不阻断） | 构建失败阻断 |
| Docker 镜像构建验证 | 分别构建后端、前端镜像，验证 Dockerfile 可用 | 是 |

### 6.2 CD（`cd.yml`）——推送到 main 或推送 `v*` 标签时触发
流水线按顺序执行，任一环节失败即停止，后续部署不会继续：

1. **测试**（单元 + 集成 + E2E，复用 MySQL 服务容器），失败则不会制作镜像；
2. **制作版本化镜像**并推送到 GHCR（`ghcr.io/<GitHub用户名>/online-teaching-platform-backend|frontend`，版本号 = 标签名如 `v1.0.0`，main 推送时 = 提交 SHA；同时打 `latest`，但镜像始终有版本号）；
3. **部署到 Kubernetes 并健康检查**：流水线内自动创建 kind 集群 → 导入镜像 → 应用 `k8s/` 部署文件 → 等待就绪 → 检查前端首页与后端 API（401/403 视为通过）；失败时自动收集 Pod 日志与状态；
4. 可选部署（仅在打 tag 且配置了对应密钥时执行）：
   - 真实 Kubernetes 集群：配置 `KUBE_CONFIG` 密钥（kubeconfig 的 base64）；
   - 服务器 docker compose：配置 `SERVER_HOST` / `SERVER_USER` / `SERVER_SSH_KEY`（见 6.3）。

每次运行的成功日志、E2E 报告、部署日志与失败诊断都会上传为构建产物（Artifacts），在 Actions 页面可随时查看。

### 6.3 启用自动部署（可选）
在 GitHub 仓库 Settings → Secrets and variables → Actions 中新增：
| Secret 名 | 说明 |
|---|---|
| KUBE_CONFIG | 真实 Kubernetes 集群的 kubeconfig（base64 编码），配置后打 tag 自动部署到 K8s |
| SERVER_HOST | 服务器 IP 或域名（配合以下三个 Secret，打 tag 自动用 docker compose 部署） |
| SERVER_USER | SSH 登录用户名 |
| SERVER_SSH_KEY | SSH 私钥（服务器上需已安装 Docker Compose 并克隆好本仓库） |
| SERVER_APP_DIR | 仓库在服务器上的绝对路径（不设置则默认 `~/online_teaching_platform`） |

### 6.4 查看结果
GitHub 仓库 Actions 页面可查看每次运行日志；E2E 报告（`04_tests/reports/e2e_report_*.md`）会上传为构建产物（Artifacts），可下载查看。

## 7. Kubernetes 部署
### 7.1 部署文件（`k8s/`）
| 文件 | 内容 |
|---|---|
| `namespace.yaml` | 命名空间 `online-teach` |
| `secret.yaml` | 数据库账号密码与 Django 密钥（演示默认值，生产务必修改） |
| `mysql.yaml` | MySQL 8 官方镜像 + 2Gi 数据卷 + 就绪/存活探针 + 服务 |
| `backend.yaml` | 后端 Deployment（镜像带版本号，就绪/存活探针）+ 媒体文件卷 + 服务 |
| `frontend.yaml` | 前端 Nginx Deployment（探针）+ NodePort 30080 服务；Nginx 配置由部署脚本从 `web_frontend/nginx.conf` 生成 ConfigMap，保持单一来源 |

镜像引用使用 `__IMAGE_TAG__` 占位符，由部署脚本替换为实际版本号，确保部署的镜像**带有版本号**（不依赖 `latest`）。

### 7.2 本地部署（Docker Desktop / Minikube / Kind / k3d 任意集群）
前置：kubectl 已连接目标集群；镜像已推送到 GHCR（或在 kind 等集群中执行 `kind load docker-image ghcr.io/...:<版本号>`）。

```bash
bash scripts/deploy_k8s.sh v1.0.0
```

脚本依次：检查/安装存储类（本地集群自动安装 local-path-provisioner）→ 应用命名空间、密钥与 MySQL → 部署后端与前端（替换镜像版本号）→ 等待就绪 → 健康检查（前端首页 200、后端 API 401/403）。任一失败立即退出，退出码非 0。

访问服务：

```bash
kubectl -n online-teach port-forward svc/frontend 8080:80
# 浏览器打开 http://localhost:8080
```

初始化测试数据与管理员：

```bash
kubectl -n online-teach exec deploy/backend -- python manage.py seed_data
kubectl -n online-teach exec deploy/backend -- python manage.py createsuperuser
```

### 7.3 部署到真实集群
流水线中配置 `KUBE_CONFIG` 密钥（kubeconfig 文件 base64 编码）后，推送 `v*` 标签时自动执行 `scripts/deploy_k8s.sh` 完成部署与健康检查（见 6.2）。
---

## 8. 微服务版部署（第二阶段，2026-08-31 起）

后端拆分为 **3 个业务微服务 + 1 个 Nginx API 网关**（划分理由见《微服务划分图.md》）：

| 服务 | 镜像（GHCR） | 库 | 职责 |
|---|---|---|---|
| user-service | `ghcr.io/jijia-hui/otp-user-service` | `otp_user` | 注册（邮箱验证码）/ JWT 登录 / 用户管理 / Django Admin |
| course-service | `ghcr.io/jijia-hui/otp-course-service` | `otp_course` | 课程 / 选课 |
| assignment-service | `ghcr.io/jijia-hui/otp-assignment-service` | `otp_assignment` | 作业 / 提交 / 评分 / 参考文档 |
| gateway（不计业务服务） | `ghcr.io/jijia-hui/otp-gateway`（K8s 中直接用 nginx:1.27-alpine + ConfigMap） | — | 按 `/api` 路径前缀路由；屏蔽 `/internal/*`；编排内服务名 `backend:8000`，前端镜像零改动 |

### 8.1 Docker Compose 启动（源码构建）

```bash
docker compose -f docker-compose.micro.yml up -d --build
docker compose -f docker-compose.micro.yml ps        # 6 个容器全部 healthy
```

- MySQL 首次启动经 `services/db/init/01-create-databases.sql` 一次创建三个库（库级隔离，杜绝跨服务联表）；
  每个服务 entrypoint 自动等待 MySQL → migrate → collectstatic → gunicorn。
- 拉取镜像免构建：`docker compose -f docker-compose.micro.prod.yml up -d`（`MICRO_IMAGE_TAG` 指定版本）。
- 与单体版（`docker-compose.yml`）容器名/卷名独立，可同时运行，供性能对比。
- 演示数据按顺序注入三个服务（见 README"微服务版快速启动"）；管理员后台仅在 user-service：`docker compose -f docker-compose.micro.yml exec user-service python manage.py createsuperuser`。

### 8.2 环境变量（微服务版新增）

| 变量 | 默认 | 说明 |
|---|---|---|
| `JWT_SECRET` | dev 值 | 用户服务签发 JWT、三服务共享验签（生产必改） |
| `JWT_EXPIRE_HOURS` | 24 | Token 有效期 |
| `INTERNAL_API_KEY` | dev-internal-key | 服务间内部接口 `X-Internal-Key`（生产必改） |
| `SERVICE_NAME` / `APP_VERSION` | — | 健康检查返回的服务名与版本号（CI 注入镜像版本） |
| `USER/COURSE/ASSIGNMENT_SERVICE_URL` | 编排内服务名 | 跨服务内部调用地址 |
| `GATEWAY_PORT` | 8000 | 网关映射到宿主机的调试端口 |
| `MICRO_IMAGE_TAG` | latest | 拉取式编排使用的镜像版本 |

### 8.3 微服务 CI/CD（`ci-micro.yml` / `cd-micro.yml`）

按**路径检测变更**：只有改动 `services/<service>/` 的服务才重新测试、构建并推送镜像（版本号 = 提交 SHA 或 `v*` 标签，另推 `latest`）；编排/部署脚本变更会触发整套重新部署。任一环节失败即停止。

CD 全链路：变更检测 →（变更服务）单元+API 测试（SQLite）→（变更服务）构建推送 GHCR → kind 集群部署（`scripts/deploy_micro_k8s.sh`，逐服务 rollout + 健康检查 + 版本号核对 + `/internal` 屏蔽校验）→ **UC01~UC14 全量 E2E 回归**（`04_tests/e2e/run_e2e_micro.py`，58 项断言）→ 上传部署日志、E2E 报告与失败诊断（Pod 状态 / deployment 详情 / 各服务日志）。

### 8.4 Kubernetes 部署（`k8s/micro/`，命名空间 `online-teach-micro`）

| 文件 | 内容 |
|---|---|
| `namespace.yaml` | 命名空间 `online-teach-micro`（与单体版隔离，两套可并行） |
| `secret.yaml` | 数据库账号、JWT 密钥、内部接口密钥 |
| `mysql.yaml` | MySQL 8 + 三库初始化 ConfigMap + 2Gi 卷 |
| `user-service.yaml` / `course-service.yaml` / `assignment-service.yaml` | 三个业务服务 Deployment（`__TAG__` 占位镜像版本、`/api/ready/` 就绪 + `/api/live/` 存活探针、`APP_VERSION` 注入、资源请求/限制、assignment 挂媒体卷）+ Service |
| `gateway.yaml` | Nginx 网关 Deployment + 服务名 `backend`（前端零改动）；配置由部署脚本从 `services/gateway/nginx.conf` 生成 ConfigMap |
| `frontend.yaml` | 前端 Deployment + NodePort 30081（单体版占 30080） |

```bash
# 每个服务可指定不同镜像版本（只滚动更新指定服务）
bash scripts/deploy_micro_k8s.sh user=<tag> course=<tag> assignment=<tag> frontend=<tag>
kubectl -n online-teach-micro port-forward svc/frontend 8080:80
```

### 8.5 运维与排障

```bash
docker compose -f docker-compose.micro.yml logs -f user-service      # 跟踪单个服务日志
curl http://localhost:8080/api/health/assignment/                    # 逐服务健康 + 版本号
kubectl -n online-teach-micro logs deploy/course-service --tail=100  # K8s 日志
kubectl -n online-teach-micro get pods                               # Pod 状态（含重启次数）
```

- **健康/就绪分离**：`/api/live/`（进程存活，不查 DB）用于 livenessProbe，`/api/ready/`（DB 可连）用于 readinessProbe，
  DB 抖动时 Pod 不会被误杀、只是摘除流量。
- **版本核对**：`/api/health/<service>/` 返回 `APP_VERSION`（= 镜像标签），现场可与 GHCR/流水线对账。
- **部署失败排查**（现场演示用）：Actions 构建产物含 `deploy_micro_k8s.log`（每个 rollout 与健康检查的输出）与失败诊断
  （`kubectl describe` + 各服务日志）。本仓库 2026-08-31 曾有一次真实失败记录：本地首次启动时三个服务全部 unhealthy，
  通过 `docker logs otp-micro-user` 看到 `Error loading MySQLdb module` → 定位为缺少 `pymysql.install_as_MySQLdb()`
  兼容垫片 → 修复后重新构建恢复。排查思路：容器状态（unhealthy）→ 单服务日志（Python 异常栈）→ 最小修复 → 重启验证。
- **故障语义**：跨服务调用失败的处理（降级/快速失败）见《跨服务调用说明.md》。
