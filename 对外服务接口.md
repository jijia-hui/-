# 对外服务接口清单

> 本文档基于第一天端到端冒烟测试脚本（`test_smoke.py`）梳理，覆盖注册、登录、课程、选课、作业、提交、评分全业务流程。  
> Base URL: `http://127.0.0.1:8000`

## 1. 认证模块 (Auth)

| 方法 | 路径 | 说明 | 请求体 | 成功状态码 | 备注 |
|------|------|------|--------|-----------|------|
| POST | `/api/auth/token/` | 用户登录获取 Token | `{username, password}` | 200 | 返回 `{token}`；未注册用户返回 400 |

## 2. 用户模块 (User)

| 方法 | 路径 | 说明 | 请求体 / 参数 | 成功状态码 | 权限 |
|------|------|------|--------------|-----------|------|
| POST | `/api/users/` | 用户注册 | `{username, password, email, is_teacher?}` | 201 | 公开 |
| GET | `/api/users/me/` | 查看当前用户信息 | - | 200 | 已登录 |
| DELETE | `/api/users/{id}/` | 删除用户（级联删除关联数据） | - | 204 | 教师/管理员 |

### 注册字段说明
- `is_teacher`: 布尔值，可选，默认 `false`。为 `true` 时注册为教师账号。

## 3. 课程模块 (Course)

| 方法 | 路径 | 说明 | 请求体 / 参数 | 成功状态码 | 权限 |
|------|------|------|--------------|-----------|------|
| POST | `/api/courses/` | 创建课程 | `{name, code, description}` | 201 | 仅教师 |
| GET | `/api/courses/` | 浏览课程列表 | - | 200 | 已登录 |
| GET | `/api/courses/{id}/` | 查看课程详情 | - | 200 | 已登录；返回 `is_enrolled` 字段 |
| DELETE | `/api/courses/{id}/` | 删除课程 | - | 204 | 仅课程创建者/管理员 |
| POST | `/api/courses/{id}/enroll/` | 学生选课 | - | 200 | 仅学生；教师选课返回 400 |

### 课程详情响应额外字段
- `is_enrolled`: 布尔值，表示当前登录学生是否已选该课程。

## 4. 作业模块 (Assignment)

| 方法 | 路径 | 说明 | 请求体 / 参数 | 成功状态码 | 权限 |
|------|------|------|--------------|-----------|------|
| POST | `/api/assignments/` | 创建作业 | `{course, title, description, deadline}` | 201 | 仅教师 |
| GET | `/api/assignments/{id}/` | 查看作业详情 | - | 200 | 已登录 |

### 作业字段说明
- `deadline`: ISO 8601 格式，如 `2026-08-30T23:59:59Z`。

## 5. 提交与评分模块 (Submission)

| 方法 | 路径 | 说明 | 请求体 / 参数 | 成功状态码 | 权限 |
|------|------|------|--------------|-----------|------|
| POST | `/api/submissions/` | 学生提交作业 | `{assignment, code}` | 201 | 仅已选课学生；未选课/教师返回 403；过期作业返回 400 |
| GET | `/api/submissions/?assignment={id}` | 查看提交记录 | Query: `assignment` | 200 | 学生仅看自己的；教师看该作业全部 |
| POST | `/api/submissions/{id}/grade/` | 教师评分 | `{score}` | 200 | 仅教师；学生返回 403；分数越界(>100)返回 400 |

### 提交状态流转
```
pending → graded

```
- 新建提交默认状态为 `pending`
- 教师评分后状态变为 `graded`，同时写入 `score` 字段

## 6. 权限矩阵总览

| 操作 | 公开 | 学生 | 教师 | 备注 |
|------|:----:|:----:|:----:|------|
| 注册 | ✅ | - | - | |
| 登录 | ✅ | - | - | |
| 创建课程 | ❌ | ❌ | ✅ | 学生尝试返回 403 |
| 选课 | ❌ | ✅ | ❌ | 教师尝试返回 400 |
| 提交作业 | ❌ | ✅(已选课) | ❌ | 未选课返回 403；教师返回 403；过期返回 400 |
| 评分 | ❌ | ❌ | ✅ | 学生尝试返回 403；分数 >100 返回 400 |
| 删除用户 | ❌ | ❌ | ✅ | 级联删除课程/作业/提交 |
| 删除课程 | ❌ | ❌ | ✅ | 学生尝试返回 403 |

## 7. 业务流程概览

```
注册 → 登录(Token) → 教师创建课程 → 学生选课
                                ↓
                        教师创建作业
                                ↓
                        学生提交作业(pending)
                                ↓
                        教师评分(graded)

```

## 8. 测试约定

- 测试账号前缀：`day1s_`，后缀为时间戳后 6 位
- 测试结束后自动清理：删除测试用户并级联删除关联数据
- 运行方式：先启动后端 `python manage.py runserver 127.0.0.1:8000`，再执行测试脚本
