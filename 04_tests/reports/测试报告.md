# 测试报告

- **项目**：在线教学与实训平台（单体版本，Git 标签 `monolith-start` 之后）
- **报告日期**：2026-08-26
- **执行人**：全组（本报告由测试脚本实际运行结果生成，非截图）

## 1. 运行环境

| 项 | 值 |
|---|---|
| 操作系统 | Windows 11（10.0.26200） |
| Python | 3.12.5（venv：`.venv`） |
| Django | 6.0.6 / Django REST Framework 3.17.1 |
| 数据库 | MySQL 8（Django 测试自动建/销毁独立测试库） |
| 后端地址 | 127.0.0.1:8000（端到端测试自动复用，不可达时自动起 8765） |

## 2. 测试结果汇总

| 层级 | 测试总数 | 通过 | 失败 | 失败原因 | 运行方式 |
|---|---|---|---|---|---|
| 单元测试（UNIT-TC01~14） | 39 | 39 | 0 | 无 | `python web_backend/manage.py test 04_tests/unit` |
| 集成/API 测试（INT-TC01~14） | 93 | 93 | 0 | 无 | `python web_backend/manage.py test 04_tests/integration` |
| 端到端测试（E2E-TC01~14） | 47 | 47 | 0 | 无 | `python 04_tests/e2e/run_e2e.py` |
| **合计** | **179** | **179** | **0** | — | — |

> 端到端明细报告（逐项断言）由脚本自动生成：`04_tests/reports/e2e_report_20260826_101421.md` 及后续每次运行的时间戳版本。

## 3. 覆盖情况

- 用例清单（UC01~UC14，v1.1 已确认）全部覆盖：注册、登录、浏览课程、选课/退课、创建课程、编辑/删除课程、管理作业（含参考文档）、作业详情、提交作业（含截止/选课/身份校验）、查看提交记录、教师查看提交、评分（含 0-100 边界）、个人信息、管理员后台。
- 每条用例覆盖主成功流程 + 备选/异常流程（详见 `02_docs/追溯表.md` 与各测试文件头注释）。
- 断言均为结果判断（状态码 + 业务数据），非"仅无报错"。

## 4. 与任务书的对应

- 单元测试：关键类、方法、业务规则、异常分支 —— `04_tests/unit/`（模型字段/默认值/排序/`is_expired`、序列化器校验/密码哈希/只读字段、权限类判定逻辑）。
- 集成/API 测试：对外接口 + 数据库访问，按用例逐一覆盖主/备选/异常流程 —— `04_tests/integration/`（每用例一个文件）。
- 端到端测试：从接口入口走完整业务流程，覆盖清单全部业务场景 —— `04_tests/e2e/run_e2e.py`（含管理员后台真实登录流程）。
- 测试失败即非零退出码，CI 流水线可在任何一步测试失败时停止构建/部署。

## 5. 本日新增/变更

- `04_tests/unit/`（新增 3 个测试文件，39 例）、`04_tests/integration/`（新增 14 个文件，93 例，原 `apps/tests.py` 的 18 例迁入并扩充）、`04_tests/e2e/run_e2e.py`（新增，47 项）。
- `web_backend/apps/models.py`：新增 `Assignment.is_expired()` 业务规则方法（视图与单测复用）。
- `web_backend/apps/views.py`：提交校验改用 `assignment.is_expired()`（行为不变）。


---

# 微服务版测试结果（第二阶段，2026-08-31）

- **对象**：后端拆分为 3 个业务微服务 + Nginx 网关（`services/`），对外 API 契约与单体版一致
- **执行方式**：本机 Docker Compose 全栈（`docker-compose.micro.yml`，6 容器全部 healthy）+ 各服务独立测试（SQLite）

## 微服务测试汇总

| 层级 | 测试总数 | 通过 | 失败 | 运行方式 |
|---|---|---|---|---|
| user-service 单元+API（SQLite+mock） | 32 | 32 | 0 | `USE_SQLITE=1 python services/user_service/manage.py test tests` |
| course-service 单元+API（SQLite+mock） | 27 | 27 | 0 | `USE_SQLITE=1 python services/course_service/manage.py test tests` |
| assignment-service 单元+API（SQLite+mock） | 37 | 37 | 0 | `USE_SQLITE=1 python services/assignment_service/manage.py test tests` |
| 端到端回归（经网关，UC01~UC14 + 级联删除） | 58 | 58 | 0 | `E2E_BASE_URL=http://127.0.0.1:8080 E2E_EXEC_PREFIX="docker exec -i otp-micro-user" python 04_tests/e2e/run_e2e_micro.py` |
| **合计** | **154** | **154** | **0** | — |

- 逐项断言明细：`04_tests/reports/e2e_micro_report_20260831_180833.md`（58/58）；
  `e2e_micro_report_20260831_180702.md`（57/58）保留为过程记录——失败项"提交响应缺 student_name"
  当场定位为 create/grade 响应未做用户名补全，修复后复测全绿。
- 微服务测试覆盖要点：跨服务调用的**降级**（用户服务不可用 → 用户名 null，数据正常返回）与
  **快速失败**（课程服务不可用 → 503，绝不绕过选课/归属校验）；删除用户/课程的跨服务级联编排的成败路径；
  内部接口密钥校验；JWT 过期/角色 claims。

## 用例 ↔ 微服务测试映射

见 `02_docs/追溯表.md` 第 6 节（UC01~UC14 逐用例对应服务、协作关系与测试文件）。

## 与任务书的对应

- "每个服务能单独构建、测试和部署"：三个服务各自独立 Django 工程/镜像/K8s Deployment/测试目录（互不依赖，测试无需数据库与其他服务）。
- "所有后端公开接口都要有 API 测试"：对外接口全部在服务级测试 + 经网关 E2E 中覆盖（含全部异常路径）。
- "清单中的全部业务场景都要做端到端回归"：`run_e2e_micro.py` 覆盖 UC01~UC14（58 项断言），并新增微服务特有的跨服务级联删除回归。
